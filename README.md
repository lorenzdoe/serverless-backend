# The Weather Archive Backend

This is the backend for the Weather Archive project. It is a serverless application that uses AWS Lambda, API Gateway, and DynamoDB. The lambdas are written in python3.10